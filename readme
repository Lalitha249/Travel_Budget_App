📂 Folder: travelbuddy
1. index.html

Purpose: Your landing page / home screen of the website.

Why: This is the first impression — visually attractive like Instagram’s vibe to pull users in.

Status: Only UI, no backend.
----------------------------------------------------------------------------------------------------------------------
2. signup.html

Purpose: Frontend page where users can create an account.

Why: Without signup, users can’t personalize their trips.

Status: Has form UI (email, password, name). Sends data to backend (signup.php).
----------------------------------------------------------------------------------------------------------------------------
3. signup.php

Purpose: Backend script for signup.

Why: Takes form data from signup.html, inserts into MySQL database (users table).

Notes:

Checks for duplicate email (so users can’t sign up twice).

Uses SQL INSERT.
----------------------------------------------------------------------------------------------------------------------------

4. login.html

Purpose: Frontend login page.

Why: Let users sign into their account.

Status: Has nice purple-pink gradient UI. Sends login request to backend (login.php).
----------------------------------------------------------------------------------------------------------------------------
5. login.php

Purpose: Backend script for login.

Why: Checks entered email & password against database (users table).

Notes:

If correct → starts PHP session → sends user to home.php.

If wrong → shows "Invalid login".
----------------------------------------------------------------------------------------------------------------------------
6. home.php

Purpose: Logged-in user’s dashboard (like Instagram feed).

Why: This is where your actual app starts (trip planning, budget saving, etc.).

Notes:

Protected with sessions → only logged-in users can see it.

Will later hold trip creation, expense tracking, AI buddy, etc.
----------------------------------------------------------------------------------------------------------------------------
7. logout.php

Purpose: Logs user out.

Why: Clears session so users can’t access home.php unless they log in again.

Status: Small but important security step.
----------------------------------------------------------------------------------------------------------------------------
8. Database (MySQL via XAMPP → phpMyAdmin)

Table: users

id (INT, auto increment, primary key)

name (VARCHAR)

email (VARCHAR, unique)

password (VARCHAR)

Why: Stores user accounts.

Status: Signup + login both work with this.
----------------------------------------------------------------------------------------------------------------------------

9. db.php

Purpose: Database connection file.

Why: Instead of writing the database connection (mysqli_connect) in every PHP file, we keep it in one place and just include it wherever needed.
-----------------------------------------------------------------------------------------------------------------------------